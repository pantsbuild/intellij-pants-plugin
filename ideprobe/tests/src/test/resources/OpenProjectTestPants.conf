include required("OpenProjectTest.conf")

pants.import.targets = ["java_app::", "python_bin::"]

project {
  name = "java_app::__python_bin::"
  modules = [
    {
      name = "java_app_main-bin",
      contentRoots = [
        { path = "java_app", kind = "sources" }
      ]
    },
    {
      name = "python_src", // is this a bug? the directory name is python_bin
      contentRoots = [
        { path = "python_bin", kind = "sources" }
      ]
    }
  ],
  pythonModules = ["python_src"]
}
