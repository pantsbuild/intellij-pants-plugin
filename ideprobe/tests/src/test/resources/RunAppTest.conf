include required("projects/ThriftExample/ideprobe.conf")

runConfiguration {
  bsp {
    mainClass = "Main"
    module.name = "java-app:main"
  }

  pants {
    mainClass = "Main"
    module.name = "java-app_main"
  }
}

expectedStdout = "ThriftStruct()\n"